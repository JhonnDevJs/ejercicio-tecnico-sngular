import { css } from "lit";

export const ModalPersonStyles = css`
	.modal-favs {
		display: var(--active);
		background-color: rgba(0, 0, 0, 0.75);
		width: 100vw;
		height: 100vh;
		z-index: 1;
	}
`;
